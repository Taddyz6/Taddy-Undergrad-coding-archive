#ifndef ARRAYUTIL_H
#define ARRAYUTIL_H

/**
 * Find the maxium value in an array
 */
int getMax (int arr[], int length);

/**
 * Find the mean value in an array
 */
double getMean (int arr[], int length);

/**
 * Find the various value in an array
 */
double getVar (int arr[], int length);

/**
 * Find the sortArray value in an array
 */
void sortArray (int arr[], int length);

#endif // ARRAYUTIL_H
